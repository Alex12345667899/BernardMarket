<h1 class="mt-5 mb-5">Show offer</h1>

  <div class="d-flex justify-content-end">

    <div class="p-2 col-6">
      <%= image_tag "bernard.png", class: "w-50 rounded"%>
    </div>

    <div class="p-2 mt-3 col-6 bg-light border">
      <p> <strong> <%= @offer.title %> 🐚</strong> </p>
      <p> <strong> <%= @offer.title %> 🐚</strong> </p>
      <p> Address: <%= @offer.location %> 🏝️ </p>
      <p> Price: <%= @offer.price %> 🌿 night </p>
      <%= render "offers/form_client_create_booking", offer: @offer, booking: @booking %>
    </div>
  </div>

  <div class="btn btn-primary">
    <%= link_to "Back home", offers_path(@offer)%>
  </div>

  <div class="mt-5 mb-5">
    <h2>Latest reviews</h2>
  </div>
