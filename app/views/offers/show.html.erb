<div class="d-flex flex-row card mt-4 bg-light" style="width: 80rem;">

  <div class="col-4">
    <%= image_tag "bernard.png", class: "w-100 rounded" %>
  </div>

  <div class="col-8 my-3 p-3">
    <h4><%= @offer.title %></h4>
    <ul>
      <li>Address: <%= @offer.location %> 🏝️</li>
      <li>Price: <%= @offer.price %> 🌿 /night <br></li>
    </ul>
    <p> Please note that by booking this shell, you acknowledge that it may not be perfectly clean and that some sand may remain in the bed. <p>

    <div>
    <div class="btn bg-sand px-20 w-30 mt-3 rounded">
      <%= link_to "Book", dashboards_bookings_path(@booking) %>
    </div>
    </div>
  </div>

</div>

<div class="mt-5 mb-5">
  <h2>Latest reviews</h2>
</div>
